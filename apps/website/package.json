{
  "name": "website",
  "private": true,
  "type": "module",
  "scripts": {
    "generate-schema": "node scripts/generate-schema.ts",
    "build": "pnpm generate-schema && linksite build linksite.config.json -f"
  },
  "dependencies": {
    "@linksite/core": "workspace:*",
    "linksite": "workspace:*",
    "zod": "catalog:"
  }
}
